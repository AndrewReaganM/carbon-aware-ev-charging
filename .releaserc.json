{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    ["@semantic-release/exec", {
      "prepareCmd": "uv version ${nextRelease.version} && jq --arg v '${nextRelease.version}' '.version = $v' custom_components/carbon_aware_ev_charging/manifest.json > manifest.tmp.json && mv manifest.tmp.json custom_components/carbon_aware_ev_charging/manifest.json"
    }],
    ["@semantic-release/git", {
      "assets": [
        "pyproject.toml",
        "uv.lock",
        "custom_components/carbon_aware_ev_charging/manifest.json"
      ],
      "message": "chore(release): ${nextRelease.version} [skip ci]"
    }],
    "@semantic-release/github"
  ]
}
